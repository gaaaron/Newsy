export class EditorMeta {
    key: string; 
    title: string; 
    dataType: string;

    constructor(key: string, title: string, dataType: string) {
        this.key = key;
        this.title = title;
        this.dataType = dataType;
    }
}